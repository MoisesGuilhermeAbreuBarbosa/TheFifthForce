'use client';
import {useState} from 'react';
import {areas,institutions,updates,videos} from '@/lib/network';
import {Select,SelectTrigger,SelectContent,SelectItem,SelectValue} from '@/components/ui/select';
import {Textarea} from '@/components/ui/textarea';
export function Network({contribute}:{contribute:(area:string)=>void}){const [area,setArea]=useState('all');return <><div className="sectionhead lower"><div><p className="eyebrow">A GLOBAL, COLLABORATIVE RESEARCH PROGRAM</p><h2>From physical principles to testable devices.</h2><p>Our long-term aim is to discover whether gravitational forces can be controlled. Progress must pass through theory, reproducible tests and physical implementation. No controllable anti-gravity effect is established here.</p><p className="ai-invitation">AI tools are welcome to read, evaluate and publish contributions through this site and its API. Disclose the model, cite sources and include reproducible methods. Human and AI contributions face the same evidence requirements.</p></div></div><div className="cards">{areas.map(a=><article className="card" key={a.id}><p className="eyebrow">{a.id}</p><h3>{a.title}</h3><p>{a.description}</p><button className="outline" onClick={()=>setArea(a.id)}>Explore this area</button> <button className="outline" onClick={()=>contribute(a.id)}>Contribute</button></article>)}</div><div className="sectionhead lower"><h2>Research updates</h2><Select value={area} onValueChange={setArea}><SelectTrigger style={{width:240}}><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All research areas</SelectItem>{areas.map(a=><SelectItem key={a.id} value={a.id}>{a.title}</SelectItem>)}</SelectContent></Select></div><p className="muted">Sources checked 5 September 2026. Publication/report dates shown below are not the dates of independent verification. Institutional listings do not imply partnership or endorsement.</p><div className="literature">{updates.filter(x=>area==='all'||x.area===area).map(x=><article key={x.id}><p className="eyebrow">{x.date} / {x.institution}</p><h3>{x.title}</h3><span className="tag">{x.evidence}</span><p>{x.summary}</p><p><strong>What it does not establish:</strong> {x.limit}</p><p><strong>Next investigation:</strong> {x.next}</p><a href={x.source} target="_blank" rel="noopener noreferrer">Primary source ↗</a>{x.context&&<> · <a href={x.context} target="_blank" rel="noopener noreferrer">Institutional context ↗</a></>}</article>)}</div><div className="sectionhead lower"><h2>Research groups worldwide</h2></div><div className="literature">{institutions.filter(x=>area==='all'||x.area===area).map(x=><article key={x.id}><p className="eyebrow">{x.region} / {x.area}</p><h3><a href={x.url} target="_blank" rel="noopener noreferrer">{x.name} ↗</a></h3><p>{x.focus}</p><p className="fine">Official research / publication entry point. No outreach has been sent.</p></article>)}</div><div className="sectionhead lower"><h2>Videos & lectures: start with a question</h2></div><p><a href="/tools/transcribe.html">Open audio transcription tool</a> · <a href="https://github.com/MoisesGuilhermeAbreuBarbosa/TheFifthForce/blob/restore/interactive-app/docs/FUNCTIONAL-AUDIT.md">Restoration audit</a></p><p><a href="/research/video-evidence.md">Download source and caption-status notes</a></p><p>Full transcripts have not been obtained. Available notes use verified metadata and primary publications; no spoken passages have been reconstructed.</p><p>Videos can introduce ideas. A claim becomes evidence only when linked to a paper, experiment or independently checkable derivation.</p><div className="cards">{videos.map(v=><article className="card" key={v.id}><p className="eyebrow">{v.creator}</p><h3><a href={v.url} target="_blank" rel="noopener noreferrer">{v.title} ↗</a></h3><span className="tag">Educational lead</span><p>{v.lesson}</p><p className="fine">{v.status}</p></article>)}</div></>}
