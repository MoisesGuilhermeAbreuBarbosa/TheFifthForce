# No database or authentication secrets are required for GitHub-backed contributions.
# Optional: enables live QAGRA AI hypothesis generation through Vercel AI Gateway.
# Without it, Quantum Advances uses a clearly labeled deterministic structured fallback.
AI_GATEWAY_API_KEY=
